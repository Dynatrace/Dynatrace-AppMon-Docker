httpd:
  container_name: httpd
  image: httpd
  ports:
  - 80
  volumes_from:
  - dtwsagent
  external_links:
  - dtcollector
  - dtwsagent
  environment:
    AGENT_NAME: httpd-agent
  command: >
    sh -c " \
      /dynatrace/run-wsagent-master.sh && \
      (echo LoadModule dtagent_module /dynatrace/agent/lib64/libdtagent.so >> conf/httpd.conf) && \
      httpd-foreground"